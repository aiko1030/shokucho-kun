// @ts-nocheck


//  function adjustArrow() {
//   const th = document.querySelector('.concern table th');
//   const arrow = document.querySelector('.arrow');

//   const thHeight = th.offsetHeight;
//   const arrowSize = thHeight; // Ajuster la taille de la flèche en fonction du parent

//   // Modifier les dimensions de la flèche
//   arrow.style.borderLeftWidth = `${arrowSize /4}px`;
//   arrow.style.borderRightWidth = `${arrowSize /4}px`;
//   arrow.style.borderTopWidth = `${arrowSize /2}px`;

//   // Positionner la flèche
//   arrow.style.right = `-${arrowSize * 3}px`;
// }

// // Appeler la fonction lors du chargement
// window.onload = adjustArrow;

// // Réajuster la flèche lors du redimensionnement de la fenêtre
// window.onresize = adjustArrow;